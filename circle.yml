machine:
  node:
    version:
      4.2.1

dependencies:
  pre:
    - npm install -g grunt-cli

test:
  pre:
    - grunt build
    - npm start 3000:
        background: true
    - npm run start-webdriver:
        background: true
    - sleep 10
  override:
    - npm test
    - npm run test-ete
